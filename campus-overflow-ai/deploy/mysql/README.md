# MySQL

Reserved for MySQL initialization files and local development notes.

Schema changes should be managed by Alembic migrations in the backend service.
Avoid maintaining hand-written schema files as the source of truth.
