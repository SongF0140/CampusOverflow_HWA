# SpeckitList 开发任务清单

> 状态：v1（2026-09-03 派生自 specs/tasks.md + specs/spec.md，按依赖排序的执行视图）
> 权威顺序仍以 specs/tasks.md 为准；本清单补充优先级、依赖、工作量预估与细化验收项。
> 优先级映射：P0 = US-01~US-10、US-15 及宪法硬性约束；P1 = US-11~US-14、US-16~US-18；P2 = US-19（第二阶段）。
> 工作量单位：**1 轮 = 一次 AI 生成会话**（含该轮内的代码 + 测试 + lint 修复）。
> 总预估：约 63 轮（T-01 已完成 3 轮，剩余约 60 轮）。

## 依赖图（关键路径）

```text
T-01(已完成) ─┬─→ T-02 ─┬─→ T-03 ─→ T-04 ─┬─→ T-05 ─┬─→ T-08
              │         │                 ├─→ T-06 ─┤   ├─→ T-09
              │         │                 └─→ T-07 ─┤   └─→ T-10
              │         │                            └──────┘
              └─→ T-11 ─→ T-12 ─┬─→ T-13 ─→ T-14
                                ├─→ T-15（另依赖 T-04/T-06/T-10）
                                └─→ T-16
T-17（P2 第二阶段，可与 T-13 并行延后）
T-18 ← T-04~T-16 ｜ T-19 ← 核心流程任务 ｜ T-20 ← 全部
```

## Stage 1：项目搭建与用户基础

- [x] **T-01 初始化三服务骨架与工具链** `P0` ｜依赖：无 ｜预估：3 轮（已完成）
  - 内容：三服务目录骨架、lint/test/dev 脚本、`/health` 端点、uv 环境、统一响应格式。
  - 验收：
    - [x] 三服务 `/health` 互通（frontend `/api/health` 聚合探测返回全 up）
    - [x] `ruff check .` 与 `npm run lint`（前端/Agent）零 error
    - [x] pytest / vitest 全部通过

- [ ] **T-02 用户注册登录与角色权限**（US-02、E-07/E-08）`P0` ｜依赖：T-01 ｜预估：5 轮
  - 内容：注册/登录/退出/资料编辑；学生/助教/教师/管理员四级角色；密码加密；关键行为日志；登录态中间件；前端登录注册页与路由守卫。
  - 验收：
    - [ ] 未登录访问受保护操作被引导至登录页（E-08）
    - [ ] 越权操作返回 403 并有中文提示（E-06）
    - [ ] 被封禁用户发帖/回答/评论/投票被拒绝并提示原因（E-07）
    - [ ] 密码以加密形式存储，日志不含明文密码
    - [ ] pytest 覆盖认证成功/失败/越权/封禁四类边界

## Stage 2：核心问答闭环（P0 主体）

- [ ] **T-03 课程模块**（US-10）`P0` ｜依赖：T-02 ｜预估：3 轮
  - 内容：课程列表/详情/加入课程；教师管理自己负责的课程；课程页聚合问题、标签、活跃用户、高频问题；前端课程页。
  - 验收：
    - [ ] 教师不能管理他人课程（管理员除外）
    - [ ] 未加入课程的学生可浏览公开内容
    - [ ] 课程页四个聚合区块在有数据时正确展示，无数据时显示空状态
    - [ ] 接口测试通过

- [ ] **T-04 问题模块**（US-03、E-01/E-02/E-10、X-03）`P0` ｜依赖：T-02、T-03 ｜预估：4 轮
  - 内容：发布/编辑/软删除问题（标题、正文、课程、标签、状态）；Markdown 渲染前 XSS 清洗；问题详情页（含问答闭环展示）；删除二次确认弹窗（Q-01）。
  - 验收：
    - [ ] 空标题/正文不能提交，按钮禁用并提示必填（E-01）
    - [ ] 内容超长被截断并提示上限（E-02）
    - [ ] 软删除后普通列表与详情不可见，管理员可追溯（E-10）
    - [ ] 含脚本内容被清洗后安全展示，不执行（X-03）
    - [ ] 删除需经确认弹窗（Q-01）

- [ ] **T-05 回答与采纳**（US-04、US-06、E-05/E-06）`P0` ｜依赖：T-04 ｜预估：4 轮
  - 内容：回答发布/编辑/软删除；提问者采纳最佳答案；采纳与声誉变更同事务；采纳答案详情页突出展示；问题状态转已解决。
  - 验收：
    - [ ] 一个问题最多一个采纳答案，再次采纳被拒绝（E-05）
    - [ ] 非提问者采纳被拒绝（E-06）
    - [ ] 采纳与积分变更在同一事务，失败一起回滚
    - [ ] 被采纳答案有显著视觉区分（清新简洁风，非蓝紫渐变）

- [ ] **T-06 评论模块**（US-05、E-01/E-10）`P0` ｜依赖：T-04、T-05 ｜预估：3 轮
  - 内容：评论问题/回答，支持二级回复；作者删自己评论、管理员删违规评论；评论触发通知。
  - 验收：
    - [ ] 空评论不能提交（E-01）
    - [ ] 被删除评论普通用户不可见，管理员可追溯（E-10）
    - [ ] 二级回复正确挂载到父评论
    - [ ] 删除需经确认弹窗（Q-01）

- [ ] **T-07 标签模块**（US-03、E-03/E-09）`P0` ｜依赖：T-04 ｜预估：3 轮
  - 内容：课程/技术/自定义三类标签；问题多标签绑定；按标签筛选与热门标签；AI 推荐标签确认后才写入的接口预留。
  - 验收：
    - [ ] 同一问题重复绑定同一标签被拒绝并提示（E-03）
    - [ ] 未经用户确认的 AI 推荐不产生任何写入（E-09）
    - [ ] 热门标签统计正确

- [ ] **T-08 投票与声誉**（US-07、US-08、E-04）`P0` ｜依赖：T-04、T-05 ｜预估：4 轮
  - 内容：问题/回答点赞点踩（可修改取消）；积分规则采纳 +15 / 点赞 +10 / 点踩 -2；积分流水表；用户主页声誉展示；周/月/课程榜（内存实现，不依赖 Redis，Q-04）。
  - 验收：
    - [ ] 同一用户对同一内容仅一个有效投票，可修改/取消（E-04）
    - [ ] 每笔积分变化必有流水记录，可对账
    - [ ] 他人积分流水不可查看（E-11）
    - [ ] 排行榜为内存实现，代码中无 Redis 依赖（Q-04）

- [ ] **T-09 搜索与筛选**（US-09）`P0` ｜依赖：T-04、T-07 ｜预估：3 轮
  - 内容：关键词搜索（标题+正文，MySQL LIKE 或 FULLTEXT）；课程/标签/时间/热度/未解决状态组合筛选；相关问题与热门列表。
  - 验收：
    - [ ] 关键词能命中标题或正文
    - [ ] 筛选条件可任意组合且结果正确
    - [ ] 加载/空/错误三态齐全

- [ ] **T-10 通知模块**（US-15、E-11）`P0` ｜依赖：T-05、T-06 ｜预估：3 轮
  - 内容：被回答/被评论/被采纳/审核结果通知；管理员待审核工单通知；标记已读；导航栏未读数。
  - 验收：
    - [ ] 通知数量与已读状态正确
    - [ ] 仅本人可见他人通知（E-11）
    - [ ] 动作（回答/评论/采纳）触发对应通知，无遗漏无重复

## Stage 3：Agent 增强与内容治理（P1）

- [ ] **T-11 Agent 服务骨架与单 Agent Loop**（C-05 基础）`P1` ｜依赖：T-01 ｜预估：3 轮
  - 内容：Hono + Vercel AI SDK 7 单 Agent Loop + task router；agent_run_id 生成；工具统一注册 `agent/src/tools/registry.ts`；前端 `@ai-sdk/react` 流式对话入口。
  - 验收：
    - [ ] 流式对话可端到端演示
    - [ ] 每次运行有唯一 agent_run_id
    - [ ] 未注册工具无法被调用
    - [ ] vitest 覆盖工具注册校验

- [ ] **T-12 内部白名单接口与服务间鉴权**（C-05）`P1` ｜依赖：T-02、T-11 ｜预估：4 轮
  - 内容：FastAPI `/internal/agent/*` 白名单接口（检索课程/问题/记忆读写/创建工单）；服务间 token 鉴权；trace id 全链路传递。
  - 验收：
    - [ ] Agent 代码库中无任何直连 MySQL 代码（grep 可查）
    - [ ] 未带凭证调用内部接口被拒绝（401）
    - [ ] 超出白名单的请求被拒绝
    - [ ] trace id 从 Agent 请求贯穿到后端日志

- [ ] **T-13 智能标签推荐与相似问题推荐**（US-11、US-12、E-09）`P1` ｜依赖：T-12 ｜预估：3 轮
  - 内容：提交前推荐相似历史问题（含链接）；提交后推荐标签（含理由与置信度）；前端确认卡片；推荐无站内依据时明确说明。
  - 验收：
    - [ ] 推荐仅建议，用户确认后才由后端写入
    - [ ] 无站内依据时明确说明而非编造
    - [ ] vitest mock 模型测试通过
    - [ ] AI 服务不可用时提问流程完全不受影响（X-02）

- [ ] **T-14 持久化记忆**（US-17、C-07、X-06）`P1` ｜依赖：T-12 ｜预估：4 轮
  - 内容：agent_memory 模块（用户偏好/课程上下文/任务经验三类）；Agent 经内部接口读写；敏感信息写入拦截；用户删除/禁用记忆入口。
  - 验收：
    - [ ] 含密码/密钥/隐私原文的写入被拒绝并留拦截日志（X-06）
    - [ ] 用户删除/禁用后记忆不再生效
    - [ ] 记忆仅经白名单接口读写，可审计
    - [ ] pytest 覆盖读写权限边界

- [ ] **T-15 内容风险预警与审批中心**（US-13、US-14、US-16、Q-06）`P1` ｜依赖：T-04、T-06、T-10、T-12 ｜预估：6 轮
  - 内容：静默检测新内容并分级（低打标 / 中高建工单附快照与原因）；审批中心七类处置（忽略/要求修改/临时隐藏/软删除/警告/限时封禁/永久封禁）；封禁记录原因、期限、操作者、快照；申诉提交与复核；前端审批中心页面。
  - 验收：
    - [ ] Agent 无法直接隐藏/删除/封号，只能创建待确认工单（Q-06、硬性约束 2）
    - [ ] 全部处置动作留审计记录（操作者/时间/快照/原因）
    - [ ] 边界测试覆盖风险分级（低/中/高）
    - [ ] 被处置用户可提交申诉，管理员复核后更新结果并通知
    - [ ] 工单处理结果通知当事人（US-15 联动）

- [ ] **T-16 观测与审计**（US-18、C-08）`P1` ｜依赖：T-11、T-12 ｜预估：3 轮
  - 内容：agent run / tool call / approval request 记录 trace id、agent_run_id、调用摘要；管理员运行记录查看页（含失败原因）；记录间相互关联。
  - 验收：
    - [ ] 按 trace id 可检索完整调用链
    - [ ] 失败运行可定位原因
    - [ ] 管理员可查、普通用户不可见

- [ ] **T-17 MCP Adapter 白名单接入**（US-19，第二阶段可延后）`P2` ｜依赖：T-12 ｜预估：3 轮
  - 内容：第一阶段用 mock MCP 工具验证 `agent/src/mcp/`（client/registry/policy/adapter）的白名单、策略与日志；第二阶段接真实 MCP Server 配置；涉文件/外部网络/发布/审核的工具必须人工确认。
  - 验收：
    - [ ] 未注册工具不可被调用
    - [ ] MCP 调用失败不影响核心问答（X-05）
    - [ ] MCP 不能直改核心业务表
    - [ ] 调用记录参数与结果摘要

## Stage 4：部署与验收

- [ ] **T-18 Docker Compose 部署** `P1` ｜依赖：T-04~T-16 全部完成 ｜预估：3 轮
  - 内容：frontend / backend / agent / MySQL 容器化；Nginx 统一入口；不含 Redis（Q-04）；deploy/ 配置。
  - 验收：
    - [ ] `docker compose up` 一键启动后三服务健康检查通过
    - [ ] 主流程可在容器环境完整演示
    - [ ] 容器内无 Redis 服务

- [ ] **T-19 测试补齐**（C-03）`P0` ｜依赖：T-02~T-16 核心流程 ｜预估：4 轮
  - 内容：认证/问答/投票/采纳/审核核心流程 pytest；Agent 工具调用 mock 测试；高风险审核边界测试；记忆权限与审批触发测试。
  - 验收：
    - [ ] `pytest` 全部通过且覆盖 C-03 列出的核心流程
    - [ ] `npm test`（前端/Agent）全部通过
    - [ ] E-01~E-11 与 X-01~X-06 在测试或验收演示中逐项可验证

- [ ] **T-20 宪法自检与收尾**（C-01/C-02/C-04）`P0` ｜依赖：全部任务 ｜预估：2 轮
  - 内容：对照 constitution.md C-01~C-08 逐条自检；UI 文案全简体中文；无蓝紫渐变；lint 零 error。
  - 验收：
    - [ ] specs/constitution.md 八条约束逐条满足
    - [ ] 界面无英文残留、无 AI 味渐变
    - [ ] 三服务 lint 零 error
    - [ ] 自检结果记录到 specs/analyze.md

## 工作量汇总

| Stage | 任务 | 轮数小计 |
| ----- | ---- | -------- |
| Stage 1 | T-01（完成）+ T-02 | 3 + 5 |
| Stage 2 | T-03~T-10 | 27 |
| Stage 3 | T-11~T-17 | 25 |
| Stage 4 | T-18~T-20 | 9 |
| **总计** | 20 任务 | **约 72 轮（剩余约 69）** |

## 执行规则（继承 tasks.md）

1. 严格按 Stage 与依赖图顺序执行，不跳序。
2. 每完成一项打勾并简述结果；失败时回改上游文档后重试。
3. T-17（P2）第一阶段可只做 mock 验证或整体延后，延期决定记入 analyze.md。
4. 单任务预估超过 6 轮时（如 T-15）应在实施前拆分为子步骤逐轮提交。
