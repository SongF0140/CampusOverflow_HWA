# 任务清单（Tasks）

> 状态：模板（待填充）
> 命令：`/speckit.tasks` → 执行用 `/speckit.implement`（支持 `--continue` 续做）
> 验收标准：每个任务都有目标与完成判定，且可按顺序执行；过大的任务必须拆分。
> 完成后在 `- [ ]` 中打勾。

## Phase 1: 项目搭建

- [ ] T-01 初始化项目结构与工具链
  - 目标：（待填写）
  - 完成判定：（示例：`npm run lint` 与 `npm run dev` 可正常运行）

## Phase 2: 核心功能

- [ ] T-02 （功能名）
  - 目标：（待填写，对应 spec 的 US-xx）
  - 完成判定：（示例：手动演示路径 + 单测通过）

## Phase 3: 数据持久化

- [ ] T-03 （存储逻辑）
  - 目标：（待填写）
  - 完成判定：（示例：刷新页面后数据不丢）

## Phase 4: 测试与优化

- [ ] T-04 编写/补充单元测试
  - 目标：核心流程均有测试
  - 完成判定：`npm test` 全部通过

- [ ] T-05 代码审查与优化
  - 目标：对照宪法逐条自检
  - 完成判定：specs/constitution.md 全部满足

## 执行说明

1. 严格按 Phase 顺序执行，任务间有依赖不得跳序。
2. 每完成一个任务打勾并简述结果。
3. 执行中失败：修复或回改上游文档后重试，不跳过。
