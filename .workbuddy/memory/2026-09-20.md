# 2026-09-20

## CampusOverflow AI 三端后端流程图设计
- 通读教学方案 PDF（76 页）与 specs/、docs/ 全部规格文档，完成现状 vs 教学方案对比分析
- 关键结论：教学方案四大子域（身份/问答/声誉/发现）全部保留；变种新增治理子域（approvals/moderation/appeals/agent_memory/observability）；角色裁剪为 学生/教师/管理员；Redis 第一阶段不引入
- 当前进度：T-01 骨架、T-02 认证权限已完成（pytest 18 项）；下一步 T-03 课程模块
- 产出四张内联流程图：三端总体接口地图、学生端、教师端、管理端后端流程（含 AI 静默检测分支、教师/管理员权限边界、申诉与审计闭环）
- 流程图设计依据：全量业务拆解蓝图第 9 节接口分组 + 第 7 节状态流转 + 宪法 C-05~C-08


## 后端 DDD 重设计（第二轮需求变更）
- 用户需求变更：助教并入学生端（研究生身份解锁助教板块，非独立角色）；本期只做后端，agent 暂缓、前端留接口
- 分析了桌面 back分析/news-headline-master（FastAPI 薄三层：router→crud→model，贫血模型，token 查库认证，无角色，Redis 缓存混在 crud，无迁移/测试）
- 可复用：Result/PageResult 统一响应、全局异常处理器、get_db 依赖注入
- 目标架构：FastAPI 按限界上下文分包（identity/qa/reputation/discovery/governance + shared_kernel/core），每包四层 interface/application/domain/infrastructure，main.py 承担装配角色
- 角色设计：RBAC 三角色（student/teacher/admin）+ 研究生身份能力位；助教标记为助教推荐徽记，不抢占提问者采纳权（E-05 不变式不变）
- 待办：spec v2 需小改（助教表述、roles 枚举、US-14 处理人）后重跑 analyze，再进入 implement


## 轻量架构定稿（第三轮）
- 用户明确要求：灵活适配、不过度工程化，输出正式架构说明
- 产出 docs/后端架构说明.md（v2 轻量适配版）：3.5 层（router/service/repository/models，放弃纯 domain 层与仓储抽象）、6 模块（identity/courses/qa/interaction/discovery/governance）、core 横切、5 条可检查 import 规则
- 关键取舍：事件总线降级为 service 显式函数调用 + TODO 钩子；reputation 并入 interaction；discovery 为纯读模块；governance 本期仅建表+501 占位
- 关键数据流：采纳闭环 = qa.service 一个事务内顺序执行 状态变更+interaction.grant_reputation+interaction.notify
- 联动：plan.md 3/7 节被本文档取代；spec.md 角色变更待改后重跑 analyze


## 分层讲解（用户自画图前的答疑）
- 讲解五层职责卡片（core 横切 / router / service / repository / models+schemas）：输入输出、只做、绝不做
- 补充穿层走位图：以采纳闭环为例，标出事务边界（service 内 3/4/5 步）、跨模块调用传 ID、异常经 core/errors.py 兜底
- 画图三规则：层间箭头只向下、service 间允许水平互指（唯一横向箭头）、agent 用虚线


## 用户自画架构图评审（第四轮）
- 用户图（draw.io 截图）整体框架对：core 四职责标注正确、5 件套抽象、工程设施独立
- 指出 6 处修正：① app→core→db→alembic→test 垂直链误解（core/db 是横切支撑非管道；alembic/tests 是工程时/横切设施）② db→module 箭头方向反了 ③ agent 画进容器内而 governance 缺失（应互换：治理占位在容器内、agent 外部虚线）④ 5 件套应放模块内部 ⑤ pyproject.toml 是元数据+依赖清单非许可证（拼写 toml 非 tonl）⑥ 错别字检索查询层、补 MySQL 落点
- 已出修正版图（运行时主链/工程设施区/core 横切三区布局）


## 规格文档同步 v3 + 教师/管理端板块图（第五轮）
- spec.md / plan.md 整文件重写为 v3：新增 US-20（研究生助教板块）、E-12（未认证访问助教板块拒绝）、E-13（助教推荐标记不影响采纳权）、Q-07（助教=研究生身份+认证，非独立角色）、Q-08（一期不搭 Agent 与治理逻辑）；US-11~14/17/18 标注第二阶段
- plan.md：模块组织以 docs/后端架构说明.md 为准，/internal/agent/* 标注二期仅留前缀，US-20 覆盖行=identity 能力位+qa 助教标记
- 需求文档.md 头部升 v3；后端架构说明.md §9 补记"已于 2026-09-20 执行完毕"
- 踩坑：对同一文件并行多次 Edit 触发 EBUSY 且互相覆盖丢改 → 同文件多处修改应整文件 Write 或逐个串行
- tasks.md / analyze.md 按用户指示暂不同步（用户：后面文档都要重写）；进 implement 前需重跑 analyze（T-02a 待插入、T-11~T-17 待标二期）
- 产出教师端/管理员端两张板块→模块映射图：教师=courses/qa/discovery/identity + governance 二期（含助教认证审核落 identity）；管理=identity/qa + governance 与外部 Agent 虚线（Agent 只创建工单）；两图均强调三端共用同一套模块、差异只在 require_roles 权限依赖


## 文档清理与重置（第六轮）
- 归档 4 项冗余文档到 `_archive/2026-09-20-pre-v3/`（可逆，附归档说明 README）：全量业务拆解与增量开发蓝图.md、docs/设计文档.md（旧大而全稿，无 v3 角色模型与阶段划分）、specs/speckit-list.md（tasks v1 派生视图）、docs/前端业务拆分/
- 重写 specs/tasks.md → v2：插入 T-02a（研究生身份与助教能力位，依赖 T-02）、T-05 增补助教推荐标记（E-13）、Phase 3（T-11~T-17）标注第二阶段、T-18 收窄为 backend+MySQL、T-19/T-20 标注一期自检范围、头部加"一期实施范围总注"
- 重写 specs/analyze.md → 第四轮：A-09（角色模型联动闭环）、A-10（范围调整闭环）、A-11（前端完成判定残留提示，低）；覆盖核对分一期/二期；最终判定=第一期允许 implement、二期启动前需重跑
- 整文件重写 AGENTS.md（三服务加阶段列、硬性约束 1~5 标二期生效/一期形态、目录权威指向后端架构说明.md）与 README.md（6 模块划分、一期范围、Agent 章节标二期、文档链接与 Roadmap 更新）
- 轻量修正 9 处残留引用/范围标注：项目骨架分析、操作文档、接口设计指南、开发协作流程文档、依赖说明加 v3 范围注；project-context.md、conventions.md 整文件重写；plan.md 第 4 节/workflow.md 示例/骨架分析目录树的"设计文档.md"引用改指后端架构说明.md
- 保留不动：constitution、spec/plan v3、需求文档、后端架构说明、workflow、设计技能使用指南、coding-style、四个占位 README、git_helper.py
- 校验通过：specs 四文档 T-02a/US-20/E-12/E-13/Q-07/Q-08 编号互通；有效文档无已归档文件残留引用；环境坑：Git Bash PATH 异常（mkdir not found），文件移动改用 PowerShell 完成


## 后端架构按端拆分文档（第七轮）
- 用户要求：docs 下建"后端架构"文件夹，架构分成三端 + agent 预留文档，且仿照 campus-overflow-ai/agent/agent端需求文档.md 的格式写（定位边界→功能需求+验收标准→接口需求表→非功能→验收清单）
- 产出 docs/后端架构/ 五份：README.md（导航 + 三端共用 6 模块底座矩阵 + 共同约定）、学生端需求文档.md（US-02~10/15/20，19 组接口表，E-12/E-13 验收）、教师端需求文档.md（课程管理 E-06、助教认证审核 Q-07 接口路径标"随 T-02a 落定回填 plan"、审批中心二期）、管理员端需求文档.md（封禁已实现、E-10 追溯、governance 501 占位、二期接口清单）、Agent端预留文档.md（一期占位清单表、C-05~C-08 规则、T-11~T-17 与 agent端需求文档章节映射）
- 关键取舍：助教认证审核/推荐标记接口路径未在 spec/plan 定死 → 文档中给出归属模块与前缀（identity=/api/users、qa=answers），标注"随 T-02a/T-05 实施落定并回填 plan 第 5 节"，不编造路径
- 联动：根 README 文档清单加"后端架构（按端拆分）"行；后端架构说明.md 头部加按端拆分入口


## 三端接口文档改写（第八轮）
- 用户两次纠偏：先要求仿 agent端需求文档 格式，后明确"要的是三端的接口文档"——最终口径：每条接口按接口设计指南第 6 节四问模板（谁调用/传什么/回什么/错了怎么办）写全；需求文档保留作验收清单交叉引用
- 产出 docs/后端架构/ 三份接口文档：学生端（19+ 接口，采纳单事务、助教推荐标记 E-13）、教师端（课程管理 E-06、助教认证审核建议路径、governance 501）、管理员端（用户治理 + E-10 追溯 + 二期清单）
- 管理员端文档以 T-02 代码为准核实（Grep backend users 模块），发现并如实记录四项实现-文档偏差：①封禁路径实为 /api/users/{id}/ban（非 /api/admin/*），需求文档表格已同步修正；②封禁即拒登录（login 403 + get_current_user 403），比 spec E-07"四类写互动被拒"更严格，学生端文档"可登录但写互动 403"表述已修正；③无期限字段（AdminUserBanRequest 仅 reason），封禁无限期直至解禁；④独立审计日志表未建。①③④整理为接口文档 1.5 差距表待后续任务澄清
- 另发现：query/body 校验错误 FastAPI 返回 422，与"六错误码"约定不符（管理员端接口文档第 5 节标注，统一异常处理时归一 400）；已实现接口响应字段蛇形 vs 指南小驼峰（学生端头部加命名说明）
- README.md 导航升级 v2：每端两份文档（接口文档为主 + 需求文档验收），格式描述改四问模板
- 环境坑：Git Bash PATH 仍损坏（tail/mkdir 均失联），全程改用 Glob/Read 代替


## 接口文档去说明化 + 需求文档退役（第九轮）
- 用户指令三件事：①"名字也要改"——三端"需求文档"退役（与接口文档并存冗余）；②每份接口文档末尾加接口总览表；③头部"依据/格式/通用约定"引用块整块去掉
- 执行：三份接口文档整文件重写——标题去"（业务后端 · 第一期）"后缀、删头部 > 说明块（节内有实质信息的 > 备注保留）、验收清单从需求文档内联进接口文档（学生端第 10 节、教师/管理员端第 6 节）、文末新增接口总览表（接口|方法|说明|权限|阶段，学生端 26 行/教师端 7 行/管理员端 9 行）
- 归档：三份需求文档移入 _archive/2026-09-20-pre-v3/（归档原因表加 3 行，注明验收清单去向）；docs/后端架构/ 现为 5 文件（README + 三端接口文档 + Agent端预留文档）
- 联动更新：后端架构 README 导航 v3（每端一行）、根 README 第 161 行措辞、后端架构说明.md 第 6 行措辞；管理员端 1.5 差距表列头去掉对已归档文件的链接

