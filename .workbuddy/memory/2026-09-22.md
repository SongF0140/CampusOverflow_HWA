# 2026-09-22

- 对比课程组《CampusOverflow软件架构设计文档_Python版.pdf》（arc42 V2.0 Python 基线）与本项目 spec v3 / plan v3：结论为"裁剪差异而非优劣"。冲突项（Redis 会话、拒绝 JWT、全链路 async、Outbox 事件）均为本项目有据可查的刻意决策，不回改；值得低成本吸收的仅有 ngram 全文索引与 OpenAPI 快照 diff。
- 与用户确立模块内分层设计哲学：**domain/service 是基座（设计好就不改、低耦合），API 与 DB 是易变外圈**。落地方向：每模块新增 domain.py（纯规则、零框架依赖）；service 出入参一律用 schemas、ORM 模型不上浮；service 内不再直接 raise HTTPException（改领域异常 + router 层统一映射）；CI 增加 lint-imports 分层契约。
- 产出《分层架构设计基线》（docs/后端架构/分层架构设计基线.md）：6 文件 × 6 模块标准、6 条设计决策（D-1~D-6）、现有资产处置表（identity 改造 / core errors 改造 / governance 与空模块保留）、施工顺序 5 步；待用户确认后回灌 后端架构说明.md 并重跑 analyze。
- 分层基线改造已落地：core/domain_error.py（纯基类）+ errors.py 统一映射；identity 试点改造完成（新增 domain.py 纯规则层、service 去 fastapi/models、repository 出参 schemas、新增 UserAuthInternal 内部模型）；courses/qa/interaction 建 6 文件占位 stub；pyproject 加 import-linter 三契约（注意：T-03+ 实现 repository 后须补 ignore_imports 行，另 app/modules 缺 __init__.py 会导致 grimp 扫不到模块）；文档回灌后端架构说明 v3 / AGENTS.md / analyze 第六轮（A-16）。验收：pytest 31 全绿、ruff 零 error、lint-imports 3 kept 0 broken。
- 教学讲解：repository/models/schemas 三层职责、service 编排+判断+事务+授权、schemas 与 models 两副面孔不可合并。现状泄漏点：identity/service.py 直接 import fastapi、sqlalchemy.orm.Session，ORM User 上浮到 service 签名。
