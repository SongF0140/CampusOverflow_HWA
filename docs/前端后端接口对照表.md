# 前端 ↔ 后端接口对照表（差异清单 + Agent 预留接口）

> 状态：v1（2026-09-20）
> 目的：按"本期只做业务后端（spec v3 / Q-08）"的边界，逐条对照**前端服务需求文档 §4 调用的接口**与**后端已实现 / 已规划的接口**，把对不上的项列成表给出处理建议；并汇总一期为 Agent 预留的空余接口。
> 数据来源：[前端服务需求文档](./前端架构/前端服务需求文档.md) §4.1~4.3、[学生端接口文档](./后端架构/学生端接口文档.md) §9、[教师端接口文档](./后端架构/教师端接口文档.md)、[管理员端接口文档](./后端架构/管理员端接口文档.md) §5、[Agent端预留文档](./后端架构/Agent端预留文档.md)、specs/tasks.md v2、backend 代码（T-02 已实现部分）。
> 使用方式：差异项处理定案后，按"回填 specs/plan.md 第 5 节 + 对应端接口文档 + 重跑 /speckit.analyze"流程修正，再进 implement。

## 0. 对接定案（2026-09-20，以"接口更多的一方"为准已修文档）

| 项 | 定案 | 落点 |
| ---- | ---- | ---- |
| D1 | 封禁 / 解禁路径统一为 `/api/users/{id}/ban·unban`（代码即权威） | 前端服务需求文档 §4.1 已改 |
| D2 | logout 两边文档均有（学生端接口文档 §9），实现为 JWT 无状态 + 前端清 Cookie | 已一致 |
| D3~D4 | 审核队列 = `GET /api/admin/moderation/cases`，审批中心 = `GET /api/admin/approvals` + `POST /{id}/resolve`（前端多出 approvals 资源，以其为准补进管理员端文档）；`/api/governance/*` 仅为一期 501 占位形态，二期 T-15 按上述路径落地 | 管理员端接口文档 §5、前端 §4.1 已改 |
| D5 | 申诉三段统一：提交 `POST /api/appeals`（学生侧）→ 列表 `GET /api/admin/appeals` → 复核 `POST /api/admin/appeals/{id}/resolve` | 学生端 / 管理员端 / 前端三处文档已改 |
| D6 / D7 / D8 | Agent 运行记录、AI 记忆、AI SSE 接口保留并标注（二期），不删除 | 前端 §4.1 已标（二期） |
| D10 | 加入 / 退出课程统一为 `POST /api/courses/{id}/join` + `DELETE /api/courses/{id}/members/me`（学生端文档形态更全，以其为准） | 前端 §4.1 已改 |
| D11~D16 | 以端接口文档为准补进前端 §4.1：课程问答区聚合、用户公开信息与声誉、回答编辑、回答 / 评论删除、助教推荐标记、课程成员列表、助教认证审核（`/api/users/assistant-certifications` 等，随 T-02a）；Q-F2 定案复用 `/api/courses/*` | 前端 §4.1 已补、Q-F2 已标定案 |
| 未定项 | ~~D9 优质内容认证接口~~、~~F1 / F2 字段命名~~（2026-09-20 已全部定案，见下）；仅剩 Q-F3 `/agent/events/poll` 方法待 Agent 成员二期确认 | —— |
| D9 | 定案：新增 `POST /api/answers/{id}/certify` / `DELETE`（取消），require_roles(teacher)、仅本人任教课程内回答，随 T-05 实现 | 前端 §4.1、教师端 / 学生端接口文档总览已补 |
| F1 / F2 | 定案：**接口字段一律蛇形**（Python/SQLAlchemy 主流），分页为 `page / page_size`；TS 代码内部仍 camelCase | 接口设计指南、开发协作流程文档、学生端 / 教师端接口文档、前端 §3.3 已同步修改 |

## 1. 快速结论

| 类别 | 数量 | 说明 |
| ---- | ---- | ---- |
| ✅ 两边一致且已实现 | 6 条 | 注册、登录、me GET/PATCH、用户列表、封禁/解禁（前缀按后端定案） |
| ✅ 两边一致、后端已规划未实现 | 13 条 | 课程 / 问题 / 回答 / 采纳 / 评论 / 标签 / 投票 / 声誉 / 搜索 / 通知（随 T-03~T-10） |
| ⚠️ 路径或形态对不上 | 10 条 | 见第 2 节差异表 |
| ⚠️ 字段与约定偏差 | 3 项 | 见第 3 节 |
| 🔒 Agent 预留（空余）接口 | 12+4+6 条 | 见第 4 节 |

## 2. 路径 / 形态对不上的接口（核心差异表）

| # | 前端调用（前端服务需求文档 §4） | 后端现状 / 规划 | 差异类型 | 处理建议 |
| -- | ---- | ---- | ---- | ---- |
| D1 | `POST /api/admin/users/{id}/ban`、`/unban`（用户管理页） | 已实现 `POST /api/users/{id}/ban`、`/unban`（require_roles(admin)，管理员端接口文档 §5 已按代码修正） | 前缀不一致 | **以代码为准**：前端文档与管理端调用改为 `/api/users/{id}/ban|unban`，不新增 `/api/admin/*` 前缀 |
| D2 | 退出登录 `POST /api/auth/logout`（顶栏用户菜单） | 后端无此接口（JWT 无状态，无服务端会话可注销） | 接口缺失 | 一期可由前端删除 Cookie 实现登出；若要服务端留"退出日志"，随 T-02 行为日志补一条 `POST /api/auth/logout`（仅记录，不做 token 吊销）——**待 SM 拍板** |
| D3 | 审核队列 `GET /api/admin/moderation/cases`（教师带 `mine=true`） | governance 占位为 `GET /api/governance/moderation/cases`（501，二期 T-15 实现）；管理员端总览表写的也是 `/api/admin/moderation/cases`（二期） | 前缀不一致 + 阶段错位 | 前缀二选一后全文档统一：建议治理类统一走 `/api/governance/*`（与模块划分一致），前端 `admin` 前缀仅是页面路径不映射接口路径；教师"本人工单"用 `?mine=true` 参数 |
| D4 | 审批工单 `GET /api/admin/approvals`、`POST /{id}/resolve` | 后端无（二期 T-15）；governance 占位用 `moderation/cases/{id}/resolve` 表述 | 前缀 + 资源名不一致 | 与 D3 一并定案：工单（moderation case）与审批（approval）是否同一资源；建议合并为 `/api/governance/*` 一套，避免两套名词 |
| D5 | 申诉 `GET /api/admin/appeals`、`POST /{id}/resolve`（申诉处理页）；学生端 `/appeals/new` 提交申诉 | 后端占位 `GET /api/governance/appeals`、`POST /api/governance/appeals/{id}/resolve`（501）；**学生端"提交申诉"接口（POST /api/appeals）在学生端总览表与前端 4.1 均未定义** | 前缀不一致 + 学生侧接口缺失 | 定案前缀（同 D3）；补一条学生侧 `POST /api/governance/appeals`（或 `/api/appeals`），随二期 T-15 实现、一期仅占位 |
| D6 | Agent 运行记录 `GET /api/admin/agent/runs`、`/{id}`（★ MVP 必经） | 后端一期无此接口（Agent 服务不搭建；审计随二期 T-16，管理员端总览表标二期） | 阶段错位 + ★ 标注过重 | 前端文档把这两条降级为二期；页面 `/admin/agent/runs` 随二期实现，一期不开发 |
| D7 | 我的 AI 记忆 `GET/PATCH/DELETE /api/users/me/memories`、`/{id}` | 后端一期无（持久化记忆随二期 T-14） | 阶段错位 | 前端 `/me/memories` 页面与接口整体标二期；一期不做 |
| D8 | AI 接口 `POST /agent/similar-questions`、`/agent/suggest-tags`（SSE）、`GET /agent/runs/{id}`（:8787，经 BFF） | Agent 服务一期不搭；BFF `api/agent/[...path]` 已就位，Agent 不可达时返回 503 | 阶段错位 | 维持 BFF 占位；前端按"AI 不可用时卡片隐藏、主流程不受影响"实现（§3.4 已有此验收） |
| D9 | 优质内容认证（教师端 P-T05，Q-F1） | 无对应接口 | 接口缺失 | 建议定案为 `POST /api/answers/{id}/certify`（require_roles(teacher)，随 T-05 实施）；或明确一期砍掉该页 |
| D10 | 课程成员 `POST / DELETE /api/courses/{id}/members` | 学生端接口文档定的是 `POST /api/courses/{id}/join` + `DELETE /api/courses/{id}/members/me` | 形态不一致 | 二选一回填 plan §5；建议按学生端接口文档的 join/members/me 形态（语义更明确） |
| D11 | 课程问答区（课程详情页问题列表）前端仅用 `/api/questions?筛选` | 学生端接口文档另有 `GET /api/courses/{id}/questions`（sort=hot 取高频） | 聚合形态差异 | 保留两条并存：通用筛选走 `/api/questions?course_id=`，课程页四聚合区块走 `/api/courses/{id}/questions`；前端文档补充后者 |
| D12 | 用户主页展示他人声誉——前端 4.1 只有 `/api/reputation/me`、`/api/reputation/rank` | 学生端接口文档有 `GET /api/users/{id}/reputation`（公开声誉摘要，E-11 限定） | 前端清单缺项 | 前端文档补 `/api/users/{id}/reputation`，随 T-08 实现 |
| D13 | 回答编辑 / 软删除——前端 4.1 未列 | 学生端接口文档有 `PATCH/DELETE /api/answers/{id}`（仅作者） | 前端清单缺项 | 前端文档补两条（问题详情页"编辑回答"按钮需要） |
| D14 | 评论只有 `GET/POST /api/questions/{id}/comments` | 后端规划还有 `/api/answers/{id}/comments`（回答评论）、`DELETE /api/comments/{id}`（作者或管理员） | 前端清单缺项 | 前端文档补齐，随 T-06 实现 |
| D15 | 助教推荐标记——前端未列 | 学生端接口文档有 `POST /api/answers/{id}/recommend`（require_graduate_assistant，E-13，随 T-05） | 前端清单缺项 | 前端文档补齐（助教身份在问题详情页展示标记按钮） |
| D16 | 教师课程管理（我的课程 / 课程管理详情）——前端复用 `/api/courses/*`（Q-F2） | 后端随 T-03 实现"教师仅本人课程可管理（E-06）"；`/api/teacher/*` 前缀未定 | 前缀待定 | 维持 Q-F2 待拍板：倾向复用 `/api/courses/*`（写操作以"教师=课程 owner"鉴权），不新开前缀 |

## 3. 字段与约定偏差

| # | 前端约定 | 后端现状 | 处理建议 |
| -- | ---- | ---- | ---- |
| F1 | ~~列表分页字段 `items / total / page / pageSize`（小驼峰）~~ | 已实现接口返回 `page / page_size`（蛇形） | ✅ 已定案（2026-09-20）：以蛇形为准，前端 §3.3 已同步 |
| F2 | ~~接口设计指南约定字段小驼峰~~ | 已实现响应为蛇形（user_id、ban_reason…） | ✅ 已定案（2026-09-20）：以蛇形为准，接口设计指南已改 |
| F3 | 错误码 400/401/403/404/500 | ~~422~~ 已于 2026-09-20 骨架改造归一为 400（含中文错误消息，tests/test_auth.py 已更新） | ✅ 已解决，无需动作 |

> 一致项（无需处理）：统一响应 `{ code, data, message }` 两边一致；登录态 BFF 注入 Authorization（前端组件不读 token）；错误码六种 + governance 占位 501。

## 4. Agent 预留（空余）接口清单

一期不实现任何 Agent 逻辑，以下为已留位、二期按 T-11~T-17 接线的接口。

### 4.1 服务间内部白名单接口（FastAPI :8000，`/internal/agent/*`，二期 T-12 注册）

| 路径 | 方法 | 用途（对应 US） |
| ---- | ---- | ---- |
| /internal/agent/questions/search | GET | 相似问题候选检索（US-12） |
| /internal/agent/questions/{id} | GET | 问题详情（含标签/回答摘要） |
| /internal/agent/courses/{id}/context | GET | 课程上下文（课程信息 + 高频问题，US-12/US-17） |
| /internal/agent/tags | GET | 标签体系（US-11） |
| /internal/agent/moderation/cases | POST | 创建审核工单（Agent 唯一处置入口，US-13，C-06） |
| /internal/agent/moderation/snapshots | POST | 内容快照（US-13） |
| /internal/agent/doc-drafts | POST | 文档草稿（二期候选，spec 未收） |
| /internal/agent/runs | POST / PATCH | 创建 / 更新 Agent 运行记录（US-18，C-08） |
| /internal/agent/tool-calls | POST | 记录工具调用摘要（US-18，C-08） |
| /internal/agent/memories | GET / POST / DELETE | 持久化记忆读写（US-17，C-07） |
| /internal/agent/approvals | POST / GET | 待确认动作（US-16，C-06） |
| /internal/agent/mcp-servers | GET | MCP Server 白名单配置（US-19） |

鉴权预留：`settings.agent_service_token`（`X-Service-Token`）已在 config 就位；`main.py` 已留前缀注释，不注册路由。

### 4.2 治理占位接口（已注册，一律 501，二期 T-15 实现）

| 路径 | 方法 | 说明 |
| ---- | ---- | ---- |
| /api/governance/moderation/cases | GET | 审核工单队列（admin） |
| /api/governance/moderation/cases/{id}/resolve | POST | 七类处置（admin） |
| /api/governance/appeals | GET | 申诉列表（admin） |
| /api/governance/appeals/{id}/resolve | POST | 申诉复核：维持 / 撤销（admin） |

配套预留：governance 模型 `ModerationCase` / `Appeal` 已建表（含 trace_id、agent_run_id 字段位）。

### 4.3 Agent 服务自身接口（:8787，二期 T-11/T-13；BFF 已通）

| 路径 | 方法 | 说明 | 前端页面 |
| ---- | ---- | ---- | ---- |
| /agent/suggest-tags | POST | 标签推荐（SSE，含理由与置信度，用户确认后写入） | 发布问题 / 问题详情 |
| /agent/similar-questions | POST | 相似问题推荐（SSE） | 发布问题 |
| /agent/moderation-scan | POST | 内容风险扫描（无界面，静默执行） | — |
| /agent/runs/{id} | GET | 运行实时状态（管理端轮询） | AGENTLOOP 实时监控 |
| /agent/events/poll | GET 或 POST | 事件轮询兜底（Q-F3 方法未定，一期前端不依赖） | — |

### 4.4 事件钩子（代码占位，二期升级为发布器）

- `QuestionPosted` / `AnswerPosted`：qa.service 发布问题 / 回答处
- `ContentFlagged`：interaction.service 内容标记处
- 形态：`# TODO(agent)` 注释位（`app/main.py` 集中标注），不改现有函数签名

## 5. 定案动作清单（给 SM / 下次评审）

1. D1：前端文档封禁路径改为 `/api/users/{id}/ban|unban`（唯一无需拍板的项，可直接改）。
2. D3~D5：治理类前缀定案（建议 `/api/governance/*`）+ 学生侧申诉提交接口补定义。
3. D2：logout 是否要服务端接口（倾向不要）。
4. ~~D9：优质内容认证 `certify` 接口定路径或砍页面~~（已定案 `POST/DELETE /api/answers/{id}/certify`）。
5. D10~D16：按学生端接口文档为准回填前端文档（形态以文档已定稿者为准）。
6. ~~F1/F2：字段命名拍板~~（已定案蛇形，指南已改）。
7. D6/D7/D8：前端文档将 Agent 相关条目标注二期，去掉 ★。
