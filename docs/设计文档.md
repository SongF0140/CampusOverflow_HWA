# CampusOverflow AI 设计文档

> 状态：草案，可继续微调  
> 本文档描述业务实现设计和技术边界。  
> 关键约束：Agent 使用 agent loop，不使用固定 workflow 作为核心抽象。

## 1. 总体架构

系统拆分为三个主要服务：

```text
frontend: Vite + React + TypeScript + Tailwind CSS
backend: FastAPI + SQLAlchemy + MySQL
agent: TypeScript + Vercel AI SDK
```

可选中间件：

```text
Redis: 缓存、限流、排行榜、Agent 任务状态
Nginx: 统一入口、反向代理、静态资源代理
Docker Compose: 本地和服务器多服务编排
```

整体调用关系：

```text
Browser
  |
  v
React Frontend
  |
  | 普通业务 API
  v
FastAPI Backend
  |
  v
MySQL / Redis

React Frontend
  |
  | AI 请求
  v
TypeScript Agent Service
  |
  | tool calling
  v
FastAPI Backend
  |
  v
MySQL / Redis
```

核心边界：

- FastAPI 是业务权威层。
- MySQL 是核心数据唯一事实来源。
- Agent 是智能辅助层，不直接连接 MySQL。
- 前端只负责展示、交互和调用 API。
- 高风险动作必须通过 FastAPI 权限校验和人工确认。

## 2. 技术选型

| 模块 | 技术 | 选择理由 |
| ---- | ---- | -------- |
| 前端框架 | Vite + React | 开发体验轻、启动快、适合 SPA |
| 前端语言 | TypeScript | 类型约束，方便复杂前端业务维护 |
| 样式 | Tailwind CSS | 快速构建现代 UI，便于统一设计 |
| 后端框架 | FastAPI | Python Web 主体，自动 OpenAPI，类型友好 |
| ORM | SQLAlchemy 2.x | 成熟 ORM，适合复杂关系模型 |
| 数据迁移 | Alembic | 管理 MySQL 表结构演进 |
| 数据库 | MySQL | 适合用户、课程、问答、投票等强关系数据 |
| Agent 服务 | TypeScript + Vercel AI SDK | 练习 TS Agent 能力，支持 tool calling、streaming、structured output |
| 缓存/限流 | Redis | 适合排行榜、AI 限流、热点缓存 |
| 部署 | Docker Compose + Nginx | 适合课程展示和服务器部署 |
| 测试 | pytest + Vitest + Playwright | 覆盖后端、前端和端到端流程 |

## 3. 服务职责划分

### 3.1 Frontend

前端负责用户界面和交互。

主要职责：

- 登录、注册、课程、问题、回答、评论、通知等页面。
- 调用 FastAPI 业务接口。
- 调用 Agent 服务展示 AI 推荐结果。
- 展示 Agent 流式输出。
- 管理本地 UI 状态。
- 对用户输入做基础校验。

不负责：

- 不保存密钥。
- 不直接连接数据库。
- 不执行核心权限判断。
- 不直接判定封禁或删除。

### 3.2 FastAPI Backend

FastAPI 是核心业务后端。

主要职责：

- 用户认证和 RBAC 权限控制。
- 用户、课程、问题、回答、评论、标签、投票、声誉、通知的 CRUD。
- MySQL 事务和数据一致性。
- OpenAPI 文档。
- 审核工单、快照、封禁、申诉管理。
- 为 Agent 暴露白名单工具 API。
- 记录审计日志。

不负责：

- 不直接承载复杂 Agent loop。
- 不让 AI 绕过权限修改核心数据。

### 3.3 TypeScript Agent Service

Agent 服务负责智能编排。

主要职责：

- 使用 Vercel AI SDK 调用模型。
- 维护 agent loop。
- 根据任务选择工具。
- 调用 FastAPI 白名单接口获取上下文。
- 生成结构化建议。
- 流式输出 AI 辅助内容。
- 自检输出是否满足格式、来源和风险要求。
- 记录 Agent 运行日志。

不负责：

- 不直接连接 MySQL。
- 不直接删帖。
- 不直接封号。
- 不直接写核心业务表。
- 不访问密钥文件或非白名单目录。

## 4. Agent Loop 设计

本项目的 Agent 不是固定 workflow。Agent loop 是一个可持续运行的智能循环。

基本循环：

```text
1. Observe: 接收用户请求或系统事件
2. Classify: 判断任务类型和风险等级
3. Plan: 规划下一步需要哪些上下文和工具
4. Act: 调用白名单工具或模型
5. Reflect: 自检结果是否可靠、是否有来源、是否越权
6. Record: 写入 agent_runs 和 tool_call_logs
7. Respond: 返回建议、流式内容或创建待审核工单
```

### 4.1 Agent 输入类型

| 输入类型 | 触发来源 | 示例 |
| -------- | -------- | ---- |
| 用户主动请求 | 前端按钮 | 推荐标签、推荐相似问题、生成辅助回答 |
| 系统事件 | FastAPI 事件通知 | 新问题、新回答、新评论 |
| 定时巡检 | Agent 定时循环 | 内容风险扫描、教师周报 |
| 管理员请求 | 后台操作 | 重新审核、生成报告、分析快照 |

### 4.2 Agent 任务类型

| 任务 | 风险 | 行为 |
| ---- | ---- | ---- |
| suggest_tags | 低 | 返回推荐标签 |
| similar_questions | 低 | 返回相似问题和原因 |
| answer_assist | 中 | 生成带来源的辅助回答 |
| moderation_scan | 高 | 创建审核快照和审核工单 |
| doc_draft | 中 | 生成文档草稿，等待确认 |
| weekly_summary | 中 | 生成教师周报 |

### 4.3 Agent 工具白名单

Agent 只能调用注册过的工具。

| 工具名 | 调用目标 | 用途 | 是否可写 |
| ------ | -------- | ---- | -------- |
| searchQuestions | FastAPI | 检索历史问题 | 否 |
| getQuestionDetail | FastAPI | 获取问题详情 | 否 |
| getCourseContext | FastAPI | 获取课程上下文 | 否 |
| listTags | FastAPI | 获取已有标签 | 否 |
| createModerationCase | FastAPI | 创建审核工单 | 是 |
| createModerationSnapshot | FastAPI | 保存审核快照 | 是 |
| createDocDraft | FastAPI 或文档服务 | 创建文档草稿 | 是 |
| getUserPublicProfile | FastAPI | 获取公开用户信息 | 否 |

高风险工具要求：

- 必须带操作者身份。
- 必须记录请求参数摘要。
- 必须记录模型判断原因。
- 不能执行永久删除。
- 不能直接执行封号。

## 5. 业务模块设计

### 5.1 用户与权限

实体：

- User
- Role
- UserBan
- AuthSession
- AuditLog

关键设计：

- 密码哈希保存。
- RBAC 权限由 FastAPI dependency 实现。
- 管理操作统一写入 audit_logs。
- 封禁用户不能创建问题、回答、评论、投票。

主要接口：

- POST /auth/register
- POST /auth/login
- POST /auth/logout
- GET /users/me
- PATCH /users/me
- POST /admin/users/{id}/ban
- POST /admin/users/{id}/unban

### 5.2 课程

实体：

- Course
- CourseMember

关键设计：

- 问题必须绑定课程。
- CourseMember 记录用户在课程中的身份。
- 教师只能管理自己负责的课程。

主要接口：

- GET /courses
- GET /courses/{id}
- POST /courses/{id}/join
- GET /courses/{id}/questions

### 5.3 问题

实体：

- Question
- QuestionTag
- Tag

关键设计：

- 软删除通过 deleted_at 或 status 实现。
- Markdown 渲染前进行 XSS 清洗。
- 问题状态包括 open、resolved、closed、hidden、deleted。
- 创建问题后触发 Agent 风险扫描事件。

主要接口：

- POST /questions
- GET /questions
- GET /questions/{id}
- PATCH /questions/{id}
- DELETE /questions/{id}

### 5.4 回答

实体：

- Answer

关键设计：

- 一个问题只能有一个 accepted_answer_id。
- 采纳答案时开启事务，同时更新问题状态和声誉流水。
- 创建回答后触发通知和风险扫描。

主要接口：

- POST /questions/{id}/answers
- PATCH /answers/{id}
- DELETE /answers/{id}
- POST /answers/{id}/accept

### 5.5 评论

实体：

- Comment

关键设计：

- 评论可以挂在问题或回答下。
- 支持 parent_id 实现二级回复。
- 评论删除采用软删除。

主要接口：

- POST /questions/{id}/comments
- POST /answers/{id}/comments
- DELETE /comments/{id}

### 5.6 投票与声誉

实体：

- Vote
- ReputationLog
- UserReputation
- Badge

关键设计：

- Vote 表对 user_id + target_type + target_id 建唯一约束。
- 声誉变化必须写入 ReputationLog。
- UserReputation 保存聚合后的当前积分。
- 排行榜可由 MySQL 查询生成，后续用 Redis 缓存。

主要接口：

- POST /votes
- DELETE /votes/{id}
- GET /users/{id}/reputation
- GET /leaderboards

### 5.7 搜索与发现

关键设计：

- 第一阶段使用 MySQL LIKE 或 FULLTEXT。
- 按课程、标签、状态、时间、热度组合筛选。
- Agent 相似问题推荐先通过 FastAPI 检索候选集，再由模型排序和解释。

主要接口：

- GET /search/questions
- GET /tags/{id}/questions
- GET /courses/{id}/questions
- POST /agent/similar-questions

### 5.8 通知

实体：

- Notification

关键设计：

- 回答、评论、采纳、审核结果都会创建通知。
- 未读数第一阶段从 MySQL 查询，后续可用 Redis 缓存。

主要接口：

- GET /notifications
- POST /notifications/{id}/read
- POST /notifications/read-all

### 5.9 内容审核

实体：

- ModerationCase
- ModerationSnapshot
- Appeal

关键设计：

- Agent 负责发现风险和创建工单。
- 管理员或教师负责最终处理。
- 快照保存原内容、风险类型、触发规则、模型原因和时间。
- 审核操作写入 audit_logs。

主要接口：

- GET /admin/moderation/cases
- GET /admin/moderation/cases/{id}
- POST /admin/moderation/cases/{id}/resolve
- POST /appeals

## 6. 数据模型草案

```text
users
- id
- username
- email
- password_hash
- role
- status
- reputation_score
- created_at
- updated_at

courses
- id
- name
- code
- description
- teacher_id
- created_at

course_members
- id
- course_id
- user_id
- member_role
- joined_at

questions
- id
- course_id
- author_id
- title
- body
- status
- accepted_answer_id
- view_count
- vote_score
- answer_count
- deleted_at
- created_at
- updated_at

answers
- id
- question_id
- author_id
- body
- vote_score
- is_accepted
- deleted_at
- created_at
- updated_at

comments
- id
- target_type
- target_id
- parent_id
- author_id
- body
- deleted_at
- created_at

tags
- id
- name
- type
- created_at

question_tags
- question_id
- tag_id

votes
- id
- user_id
- target_type
- target_id
- value
- created_at
- updated_at

reputation_logs
- id
- user_id
- source_type
- source_id
- delta
- reason
- created_at

notifications
- id
- user_id
- type
- title
- body
- link
- is_read
- created_at

agent_runs
- id
- task_type
- trigger_type
- status
- input_summary
- output_summary
- risk_level
- started_at
- finished_at

tool_call_logs
- id
- agent_run_id
- tool_name
- arguments_summary
- result_summary
- status
- created_at

moderation_cases
- id
- target_type
- target_id
- risk_level
- reason
- status
- created_by_agent_run_id
- reviewer_id
- resolved_at
- created_at

moderation_snapshots
- id
- case_id
- target_type
- target_id
- content_snapshot
- metadata_json
- created_at

appeals
- id
- user_id
- case_id
- reason
- status
- created_at
- resolved_at
```

## 7. Agent 接口草案

Agent 服务对前端或 FastAPI 暴露：

| 接口 | 方法 | 说明 |
| ---- | ---- | ---- |
| /agent/suggest-tags | POST | 推荐问题标签 |
| /agent/similar-questions | POST | 推荐相似问题 |
| /agent/answer-assist | POST | 生成辅助回答 |
| /agent/moderation/scan | POST | 扫描内容风险 |
| /agent/docs/draft | POST | 生成文档草稿 |
| /agent/runs/{id} | GET | 查询 Agent 运行状态 |

FastAPI 对 Agent 暴露白名单接口：

| 接口 | 方法 | 说明 |
| ---- | ---- | ---- |
| /internal/agent/questions/search | GET | 检索问题候选集 |
| /internal/agent/questions/{id} | GET | 获取问题详情 |
| /internal/agent/courses/{id}/context | GET | 获取课程上下文 |
| /internal/agent/tags | GET | 获取标签列表 |
| /internal/agent/moderation/cases | POST | 创建审核工单 |
| /internal/agent/moderation/snapshots | POST | 创建审核快照 |
| /internal/agent/doc-drafts | POST | 创建文档草稿 |

内部接口要求：

- 使用服务间 token。
- 只返回 Agent 必要字段。
- 不返回密码、邮箱验证码、密钥、私密日志等敏感信息。
- 写接口必须记录 agent_run_id。

## 8. 安全设计

### 8.1 权限边界

- 普通用户只能管理自己的问题、回答和评论。
- 教师只能管理负责课程下的部分内容。
- 管理员拥有全局治理权限。
- Agent 没有管理员身份，只能创建建议、草稿、工单和快照。

### 8.2 Agent 安全

- Agent 工具白名单注册。
- 工具调用记录日志。
- 高风险操作需要人工确认。
- Agent 不读取 .env、密钥、数据库备份和非白名单文件。
- Agent 输出需要结构化校验。
- AI 辅助回答必须标注来源和 AI 属性。

### 8.3 内容安全

- 用户输入做长度限制。
- Markdown 输出做 HTML 清洗。
- 审核快照保留原始证据。
- 删除采用软删除，保留审计链路。

## 9. 部署设计

推荐 Docker Compose 服务：

```text
frontend
backend
agent
mysql
redis
nginx
```

Nginx 路由建议：

```text
/              -> frontend
/api/          -> backend
/agent/        -> agent
/docs/api      -> backend OpenAPI
```

环境变量：

- DATABASE_URL
- REDIS_URL
- AGENT_SERVICE_TOKEN
- LLM_API_KEY
- JWT_SECRET 或 SESSION_SECRET

## 10. 测试设计

后端测试：

- 用户注册登录。
- RBAC 越权拒绝。
- 发布问题、回答、采纳答案。
- 投票唯一约束。
- 声誉流水一致性。
- 审核工单创建。

前端测试：

- 登录表单。
- 问题发布表单。
- 问题详情交互。
- Agent 推荐结果展示。

Agent 测试：

- 工具参数校验。
- 模型输出结构校验。
- 低风险任务直接返回建议。
- 高风险任务只能创建工单，不能执行封号。

端到端测试：

```text
注册登录
→ 加入课程
→ 发布问题
→ Agent 推荐标签
→ 回答问题
→ 采纳答案
→ 声誉变化
→ 通知出现
```

## 11. 风险与应对

| 风险 | 影响 | 应对 |
| ---- | ---- | ---- |
| Agent 范围过大 | 项目难以完成 | 第一阶段只做标签、相似问题、审核预警 |
| AI 误判内容 | 伤害用户体验 | AI 只预警，人工最终处理 |
| Agent 越权 | 安全风险 | 工具白名单、服务 token、日志审计 |
| 数据一致性复杂 | 积分和采纳出错 | 事务处理、积分流水、唯一约束 |
| 前后端服务过多 | 部署复杂 | Docker Compose 统一编排 |
| Redis 引入过早 | 增加负担 | 第一阶段可选，必要时再加 |

## 12. 推荐实现顺序

1. 搭建前端、后端、Agent 三服务骨架。
2. 完成用户认证和课程模块。
3. 完成问题、回答、评论、标签。
4. 完成采纳、投票、声誉流水。
5. 完成基础搜索和通知。
6. 完成 Agent 智能标签和相似问题推荐。
7. 完成 Agent 内容风险预警、快照和审核工单。
8. 完成 Docker Compose 部署。
9. 补充测试、README、ADR 和演示数据。
