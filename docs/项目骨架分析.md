# CampusOverflow AI 项目骨架分析

> 状态：草案，可继续微调  
> 目标：为后续创建代码仓库、分工和任务拆解提供目录依据。

## 1. 骨架设计原则

项目包含前端、业务后端、Agent 服务和部署配置。目录应让团队成员一眼看清每个服务的职责边界。

原则：

- 前端、后端、Agent 服务物理分离。
- 核心业务代码按领域模块组织。
- Agent 工具、循环、提示词和日志独立组织。
- 文档、部署、脚本和测试放在固定位置。
- 不把 AI 能力散落在普通 CRUD 模块里。

## 2. 推荐顶层目录

工作区根目录存放文档、规格与工程规则；代码服务统一放在 `campus-overflow-ai/` 总目录下。

```text
workplace/
├── campus-overflow-ai/    # 项目代码总目录
│   ├── frontend/          # Next.js + React 19 + TypeScript 前端/BFF/AI UI
│   ├── backend/           # FastAPI 业务后端
│   ├── agent/             # TypeScript + Vercel AI SDK Agent 服务
│   ├── deploy/            # Docker、Nginx、服务器部署配置
│   ├── scripts/           # 开发、测试、部署辅助脚本
│   ├── tests/             # 跨服务端到端测试，可选
│   ├── docker-compose.yml
│   └── .env.example
├── docs/                  # 项目文档、ADR、接口说明、Sprint 总结
├── specs/                 # 规格驱动文档（constitution/spec/plan/tasks/analyze）
├── .trae/                 # Trae 项目规则
└── README.md
```

## 3. Frontend 骨架

```text
frontend/
├── package.json
├── next.config.ts
├── tsconfig.json
├── postcss.config.mjs
├── next-env.d.ts
└── src/
    ├── app/               # Next.js App Router 页面、布局和轻量 Route Handlers
    │   ├── layout.tsx
    │   ├── page.tsx
    │   ├── auth/
    │   ├── courses/
    │   ├── questions/
    │   ├── admin/
    │   ├── agent/
    │   └── api/           # 仅放 BFF/AI UI 转发，不放核心业务
    ├── features/          # 按业务功能组织
    │   ├── auth/
    │   ├── courses/
    │   ├── questions/
    │   ├── answers/
    │   ├── comments/
    │   ├── tags/
    │   ├── reputation/
    │   ├── notifications/
    │   ├── moderation/
    │   └── agent-assist/
    ├── shared/
    │   ├── components/
    │   ├── hooks/
    │   ├── utils/
    │   └── types/
    ├── api/               # FastAPI 和 Agent API 客户端
    └── styles/
```

### 3.1 Frontend 模块说明

| 目录 | 职责 |
| ---- | ---- |
| app/auth | 登录、注册、个人资料页面 |
| app/courses | 课程列表、课程详情 |
| app/questions | 问题列表、问题详情、发布问题 |
| app/admin | 用户管理、课程管理、审核管理 |
| app/agent | Agent 运行记录、文档草稿等界面 |
| features/agent-assist | 智能标签、相似问题、辅助回答 UI |
| api | 封装 backend 和 agent 请求 |

### 3.2 前端状态建议

第一阶段可以不引入复杂状态库。Next.js 负责路由、布局和服务端渲染能力，复杂业务状态仍然放在客户端组件或领域 hooks 中。

建议：

- 登录用户信息：Context 或轻量状态库。
- 服务端数据：优先用 Next.js 服务端 fetch 或封装客户端 hooks。
- 表单状态：组件内部状态。
- Agent 流式结果：独立 hook 管理。

如果后续客户端交互复杂度上升，再考虑 TanStack Query。

## 4. Backend 骨架

```text
backend/
├── pyproject.toml
├── alembic.ini
├── app/
│   ├── main.py
│   ├── core/
│   │   ├── config.py
│   │   ├── security.py
│   │   ├── permissions.py
│   │   ├── logging.py
│   │   └── errors.py
│   ├── db/
│   │   ├── session.py
│   │   ├── base.py
│   │   └── migrations/
│   ├── modules/
│   │   ├── auth/
│   │   ├── users/
│   │   ├── courses/
│   │   ├── questions/
│   │   ├── answers/
│   │   ├── comments/
│   │   ├── tags/
│   │   ├── votes/
│   │   ├── reputation/
│   │   ├── search/
│   │   ├── notifications/
│   │   ├── moderation/
│   │   └── agent_gateway/
│   └── shared/
│       ├── pagination.py
│       ├── schemas.py
│       └── events.py
└── tests/
    ├── conftest.py
    ├── test_auth.py
    ├── test_questions.py
    ├── test_reputation.py
    └── test_moderation.py
```

### 4.1 后端模块内部结构

每个业务模块建议保持类似结构：

```text
questions/
├── models.py       # SQLAlchemy 模型
├── schemas.py      # Pydantic 请求/响应模型
├── service.py      # 业务规则和事务
├── router.py       # FastAPI 路由
├── repository.py   # 数据访问封装，可选
└── events.py       # 领域事件，可选
```

### 4.2 后端模块职责

| 模块 | 职责 |
| ---- | ---- |
| auth | 登录、注册、会话、密码 |
| users | 用户资料、状态、封禁 |
| courses | 课程和课程成员 |
| questions | 问题发布、编辑、软删除 |
| answers | 回答、采纳 |
| comments | 评论和二级回复 |
| tags | 标签和问题标签关系 |
| votes | 点赞、点踩、防重复投票 |
| reputation | 积分流水、声誉聚合、徽章 |
| search | 关键词搜索、筛选排序 |
| notifications | 站内通知 |
| moderation | 审核工单、快照、申诉 |
| agent_gateway | 给 Agent 暴露的内部白名单接口 |

## 5. Agent 骨架

```text
agent/
├── package.json
├── tsconfig.json
├── src/
│   ├── server.ts              # Agent 服务入口
│   ├── config.ts              # 环境变量和配置
│   ├── loop/
│   │   ├── agent-loop.ts       # Observe -> Plan -> Act -> Reflect -> Record
│   │   ├── task-classifier.ts
│   │   ├── risk-policy.ts
│   │   └── self-check.ts
│   ├── tools/
│   │   ├── registry.ts         # 工具白名单注册
│   │   ├── backend-client.ts   # 调用 FastAPI 内部接口
│   │   ├── search-questions.ts
│   │   ├── get-question-detail.ts
│   │   ├── get-course-context.ts
│   │   ├── create-moderation-case.ts
│   │   ├── create-snapshot.ts
│   │   └── create-doc-draft.ts
│   ├── tasks/
│   │   ├── suggest-tags.ts
│   │   ├── similar-questions.ts
│   │   ├── answer-assist.ts
│   │   ├── moderation-scan.ts
│   │   ├── weekly-summary.ts
│   │   └── doc-draft.ts
│   ├── prompts/
│   │   ├── suggest-tags.ts
│   │   ├── similar-questions.ts
│   │   ├── answer-assist.ts
│   │   └── moderation-scan.ts
│   ├── routes/
│   │   ├── suggest-tags.ts
│   │   ├── similar-questions.ts
│   │   ├── answer-assist.ts
│   │   ├── moderation.ts
│   │   └── runs.ts
│   └── types/
│       ├── task.ts
│       ├── tool.ts
│       └── agent-run.ts
└── tests/
    ├── tool-registry.test.ts
    ├── self-check.test.ts
    └── moderation-policy.test.ts
```

## 6. Agent Loop 文件职责

| 文件 | 职责 |
| ---- | ---- |
| agent-loop.ts | 串起感知、规划、工具调用、自检和记录 |
| task-classifier.ts | 判断任务类型，例如标签推荐、审核扫描 |
| risk-policy.ts | 判断风险等级和允许动作 |
| self-check.ts | 校验模型输出是否符合结构、来源和安全要求 |
| registry.ts | 注册允许调用的工具 |
| backend-client.ts | 封装和 FastAPI 的服务间调用 |

关键点：

- loop 是运行模型，不是一次性函数集合。
- task 是 loop 可处理的任务类型，不是固定 workflow 节点。
- tools 是 Agent 可以选择的动作集合。
- risk-policy 决定 Agent 能不能执行某个动作。

## 7. Docs 骨架

```text
docs/
├── 需求文档.md
├── 设计文档.md
├── 项目骨架分析.md
├── adr/
│   ├── ADR-001-技术选型.md
│   ├── ADR-002-Agent边界.md
│   └── ADR-003-内容审核策略.md
├── api/
│   └── openapi-notes.md
├── sprint/
│   ├── sprint-0.md
│   ├── sprint-1.md
│   └── sprint-2.md
└── reports/
    ├── test-report.md
    └── deployment-report.md
```

## 8. Deploy 骨架

```text
deploy/
├── nginx/
│   └── nginx.conf
├── mysql/
│   └── init.sql
├── redis/
│   └── redis.conf
├── backend.Dockerfile
├── frontend.Dockerfile
├── agent.Dockerfile
└── docker-compose.prod.yml
```

建议服务：

```text
frontend
backend
agent
mysql
redis
nginx
```

### 8.1 端口与访问入口

学生端、教师端和管理员后台复用同一个 Next.js 服务。管理员后台只是 `app/admin` 下的一组页面，不单独开端口。

| 服务 | 端口 | 说明 |
| ---- | ---- | ---- |
| frontend | `3000` | Next.js，承载学生端、教师端、管理员后台、Agent 面板 |
| backend | `8000` | FastAPI，承载业务 API、管理员 API、Agent 内部 API |
| agent | `8787` | TypeScript Agent Runtime |
| mysql | `3306` | 仅 Docker 内网访问 |
| redis | `6379` | 可选，仅 Docker 内网访问 |
| nginx | `80/443` | 生产环境统一入口 |

Nginx 对外路由：

```text
/              -> frontend:3000
/api/          -> backend:8000
/agent/        -> agent:8787
/docs/api      -> backend:8000/docs
```

内部路由：

```text
agent -> backend:8000/internal/agent/*
```

`/internal/agent/*` 只允许服务间访问，不对公网开放。

## 9. API 路径规划

面向前端的业务 API：

```text
/api/auth/*
/api/users/*
/api/courses/*
/api/questions/*
/api/answers/*
/api/comments/*
/api/tags/*
/api/votes/*
/api/reputation/*
/api/search/*
/api/notifications/*
/api/admin/moderation/*
```

面向 Agent 的内部 API：

```text
/internal/agent/questions/search
/internal/agent/questions/{id}
/internal/agent/courses/{id}/context
/internal/agent/tags
/internal/agent/moderation/cases
/internal/agent/moderation/snapshots
/internal/agent/doc-drafts
```

面向前端的 Agent API：

```text
/agent/suggest-tags
/agent/similar-questions
/agent/answer-assist
/agent/moderation/scan
/agent/docs/draft
/agent/runs/{id}
```

## 10. 分工建议

| 方向 | 适合成员 | 主要任务 |
| ---- | -------- | -------- |
| 前端 | 熟悉 React/TS 的成员 | 页面、组件、API 调用、AI 输出展示 |
| 后端业务 | 熟悉 Python/MySQL 的成员 | FastAPI、SQLAlchemy、业务规则 |
| Agent | 想练 TS + AI SDK 的成员 | agent loop、tool calling、模型输出 |
| 部署测试 | 工程能力较强的成员 | Docker、Nginx、CI/CD、测试 |
| 产品文档 | 沟通表达较强的成员 | 需求、ADR、Sprint 总结、演示材料 |

## 11. 第一阶段最小骨架

第一阶段不需要一次性建满所有目录。推荐最小骨架：

```text
campus-overflow-ai/
├── frontend/
├── backend/
├── agent/
├── deploy/
└── docker-compose.yml
```

每个服务只先保留能启动、能健康检查、能互相调用的最小代码。

第一阶段验收：

- 前端能打开首页。
- 后端能返回 /health。
- Agent 能返回 /health。
- 前端能调用后端接口。
- 前端能调用 Agent 接口。
- Agent 能调用 FastAPI 内部测试工具。
- Docker Compose 能启动所有基础服务。

## 12. 后续微调点

以下开放点已做出初期决策（实现中可再评估）：

- ~~是否使用 monorepo 包管理工具~~ → 第一阶段不引入，三个服务独立 install；多服务联调用根目录脚本。
- ~~前端是否引入 TanStack Query~~ → 第一阶段不引入，用 fetch hooks；复杂度上升再评估。
- ~~后端是否使用 async SQLAlchemy~~ → 第一阶段使用同步 SQLAlchemy + PyMySQL，稳定优先。
- ~~Agent 服务使用 Express、Hono 还是原生 Node server~~ → 选定 Hono（轻量、原生支持 SSE、边缘友好）。
- Agent 是否需要独立任务队列 → 第一阶段不需要，站内事件同步消费；量级上来再引入。
- ~~Redis 是否第一阶段就进入 docker-compose~~ → 不进入，Redis 为可选依赖组，第一阶段用内存限流替代。
- 管理后台是独立 React 页面还是复用主前端布局 → 复用主前端布局（app/admin）。
