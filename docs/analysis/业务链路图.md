# CampusOverflow AI 业务链路图

> Issue：#5 analysis engineer
> 定位：从系统视角画出"用户请求 → 前端 → 业务后端 → 数据"的端到端链路，以及 Agent 增强链路和治理链路。流程细节见 [业务流程分析.md](./业务流程分析.md)。

## 1. 端到端总体链路

```mermaid
flowchart TB
    subgraph Users["用户侧"]
        G[游客]
        S[学生]
        T[教师/助教]
        ADM[管理员]
    end

    subgraph FE["Next.js 前端 (App Router)"]
        PG["页面 (Server Components)<br/>src/app/**"]
        FT["业务组件与 Hooks<br/>src/features/**"]
        BFF["BFF 路由 (src/app/api/**)<br/>鉴权上下文传递 / SSE 透传"]
        PG --> FT --> BFF
    end

    subgraph BE["FastAPI 业务后端"]
        AUTH["认证与 RBAC"]
        MOD["业务模块<br/>users / courses / questions / answers /<br/>comments / tags / votes / notifications"]
        GOV["治理模块<br/>moderation / approvals / appeals / audit"]
        INT["内部白名单接口<br/>/internal/agent/*"]
        AUTH --> MOD
        MOD --> DB[("MySQL<br/>SQLAlchemy 2.x")]
        GOV --> DB
        INT --> DB
    end

    subgraph AG["Agent 服务 (TypeScript)"]
        LOOP["Agent Loop + task router"]
        TLS["工具注册表 registry.ts"]
        MEM["持久化记忆 memory/"]
        MCP["MCP Adapter (白名单)"]
        OBS["观测 observability/<br/>agent_run_id / tool_call_log / trace id"]
        LOOP --> TLS
        LOOP --> MEM
        LOOP --> MCP
        LOOP --> OBS
    end

    G & S & T & ADM --> PG
    BFF -->|"REST { code, data, message }"| AUTH
    BFF -->|"AI 流式请求 (SSE)"| LOOP
    LOOP -->|"只调白名单接口 + 服务间 token + x-trace-id"| INT
    INT --> APM[("agent_memory / agent_run / tool_call_log")]
```

## 2. 分链路说明

### L1 问答主链路

```mermaid
flowchart LR
    U[学生] -->|提问/回答/评论/投票| F[前端页面]
    F -->|BFF 转发 + JWT 上下文| B[FastAPI 业务模块]
    B -->|事务写入| DB[(MySQL)]
    B -->|通知事件| N[notifications 表]
```

- 前端不直接访问数据库，所有业务请求经 BFF 携带鉴权上下文转发。
- FastAPI 统一返回 `{ code, data, message }`，写操作全部经过 RBAC 校验。

### L2 AI 增强链路

```mermaid
flowchart LR
    U[用户] -->|提问前/发布时| F[前端 AI UI]
    F -->|SSE 流式| A[Agent Runtime]
    A -->|检索上下文| I[/internal/agent/* 白名单接口/]
    I --> DB[(MySQL)]
    A -->|只返回建议| F
    F -->|用户确认后提交| B[FastAPI 业务写入]
```

- Agent 只"建议"不"写入"：标签推荐、相似问题、辅助回答均由用户确认后经 FastAPI 正常业务接口落库。
- AI 生成内容必须标识来源，不得冒充教师/助教。

### L3 内容治理链路

```mermaid
flowchart LR
    P[新内容发布] --> S[FastAPI 保存]
    S --> D[Agent 静默检测]
    D -->|中高风险| T[审核工单 + 内容快照]
    T --> H[管理员/教师审批]
    H -->|确认执行| E[FastAPI 执行处置<br/>隐藏/软删除/封禁]
    E --> N[通知用户] --> AP[申诉入口]
```

- 高风险动作（删帖、封号、隐藏、文件写入、外部 MCP 调用）一律"先工单、后人工、再执行"。
- 快照、操作者、原因、期限全程留痕，支持审计与申诉。

### L4 观测与记忆链路

```mermaid
flowchart LR
    RUN[Agent run] -->|trace id 贯穿| T1[tool_call_log]
    RUN --> T2[approval request]
    RUN --> T3[moderation case]
    RUN --> M[(agent_memory<br/>经 FastAPI 内部接口读写)]
    T1 & T2 & T3 --> ADM2[管理员后台: Agent 运行记录]
```

- 每次运行、工具调用、审批请求都携带 trace id 与摘要，可关联、可回放。
- 记忆读写只经 FastAPI 内部接口，写入前后做敏感信息过滤（不含密码、密钥、隐私原文）。

## 3. 关键边界约定

| 边界 | 约定 |
| ---- | ---- |
| 前端 ↔ 数据库 | 禁止直连，必须经 BFF → FastAPI |
| Agent ↔ MySQL | 禁止直连，只经 `/internal/agent/*` 白名单接口 |
| Agent ↔ 高风险动作 | 只生成审批工单，人工确认后由 FastAPI 执行 |
| MCP 工具 | 必须白名单注册，经 registry / policy / adapter 三层 |
| 跨服务调用 | 必须携带 `x-trace-id`，统一错误码 |
